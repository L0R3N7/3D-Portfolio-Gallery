export class Theme{
    id : number;
    thumbnail_url: number;
    
    constructor(id: number, thumbnail_url: number) {
        this.id = id;
        this.thumbnail_url = thumbnail_url

        
      }
}